[package]
name = "waecan-p2p"
version.workspace = true
edition.workspace = true

[dependencies]
libp2p = { version = "0.53", features = [
    "tcp", "noise", "yamux", "kad", "gossipsub", "identify",
    "tokio"
]}
tokio = { version = "1.38", features = ["full"] }
